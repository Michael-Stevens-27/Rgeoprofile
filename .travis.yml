# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
sudo: required
cache: packages

r_packages: devtools

addons:
  apt:
    packages:
      - libgdal-dev
      - libproj-dev
      - libfftw3-dev
